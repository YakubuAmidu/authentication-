import React, {Component} from 'react';
import {Button, Card, CardSection} from 'react-native';

class LoginForm extends Component {
  render() {
    return <View />;
  }
}

export default LoginForm;
